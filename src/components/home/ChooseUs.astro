---
import Container from "../ui/Container.astro";
import Title from "../ui/Title.astro";
import Paragraph from "../ui/Paragraph.astro";
import Button from "../ui/Button.astro";
import layout from "@assets/layout.png?url";
import logo from "@assets/logos/logolight.svg?url";
import { t } from "i18next";
import { Trans } from "astro-i18next/components";
---

<Container>
  <div
    class="grid grid-cols-1 xl:grid-cols-[750px_1fr] justify-between gap-10 lg:gap-20">
    <div
      class="flex flex-col items-start gap-3 p-2 lg:p-6 w-[min(1400px,100%)] mx-auto">
      <Title>{t("choose.title")}</Title>
      <Paragraph>
        <Trans i18nKey="choose.paragraph">
          {t("choose.paragraph")}
        </Trans>
      </Paragraph>

      <Button action="contact" className="mt-4 lg:mt-7">{t("action.join")}</Button>
    </div>
    <div
      class="max-[900px]:hidden relative pt-10 pl-4 max-w-[800px] mx-auto bg-primary/10 rounded-2xl flex flex-col">
      <img
        loading="lazy"
        src={logo}
        alt="logo"
        class="h-24 absolute left-4 top-4"
      />
      <img
        loading="lazy"
        src={layout}
        alt="ventrad leyout page"
        class="h-full w-full object-cover relative z-10"
      />
    </div>
  </div>
</Container>
